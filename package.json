{
  "name": "plan",
  "version": "3.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cd schedule-data-service && node index",
    "init-sds": "cd schedule-data-service && npm install && node index",
    "init-adm": "cd admin && npm install && vite build",
    "cy-test": "npx cypress open",
    "init-newman": "cd api_test && npm install -g newman && newman run Teacher_CRUD.postman_collection.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "archiver": "^5.2.0",
    "compression": "^1.7.4",
    "express": "^4.18.1",
    "express-session": "^1.17.1",
    "newman": "^5.3.2",
    "nodemailer": "^6.4.17",
    "pdfkit": "^0.11.0"
  }
}

